function main() => void {
    localvar x: integer;
    localvar y: integer;
    
    x = 10;
    y = 5;

    if (x > y) then {
        write(x); 
    } else {
        write(y); 
    };
    
    if (x > y) then {
        if (x >= y) then {
        write(x + y); 
    } else {
        write(x); 
    }; 
    } else {
        write(y); 
    };
    
    if (x == y) then {
        write(x);
    } else ;
    
    if (x <> y) then {} else {
        write(x); 
    };
}