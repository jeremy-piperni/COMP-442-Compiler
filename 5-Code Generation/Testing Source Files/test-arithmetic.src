function main() => void {
    localvar x: integer;
    localvar y: integer;
    localvar z: integer;

    x = 10;
    y = 5;
    
    z = x + y;
    write(z);
    z = x - y;
    write(z);
    z = x * y;
    write(z);
    z = x / y;
    write(z);
}